version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=PrivateMessaging_Identity
      - PrivateMessaging_DB=PrivateMessaging_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
