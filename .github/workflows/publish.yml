name: publish to nuget
on:
  push:
    branches:
      - master # Default release branch
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Setup .NET Core
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 3.1.200
          source-url: https://nuget.pkg.github.com/EasyAbp/index.json
        env:
          NUGET_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}

      - name: dotnet build
        run: dotnet build -c Release

      - name: dotnet pack
        run: dotnet pack -c Release --no-build -o dest

      - name: go to dest directory
        run: cd dest

      - name: remove unused packages
        run: |
          ls | grep -v EasyAbp. | xargs rm -f
          ls | grep -v HttpApi.Client.ConsoleTestApp | xargs rm -f

      - name: dotnet nuget push to GitHub
        run: dotnet nuget push *.nupkg --skip-duplicate --no-symbols true

      - name: dotnet nuget push to NuGet
        run: dotnet nuget push *.nupkg -k ${{secrets.NUGET_API_KEY}} -s https://api.nuget.org/v3/index.json --skip-duplicate